# English texts; also the fallback for missing translations. Keep every locale's keys in sync.

language_name = "English"

[tray]
suspend = "Suspend"
reload = "Reload settings"
rehook = "Reinstall the input hooks"
settings = "Open settings"
exit = "Exit"
suspended = " (suspended)"
passthrough = " (passthrough)"

[notice]
undefined = "%{keys} is undefined"
timed_out = "%{keys} timed out"
mode = "Mode: %{mode}"
count = "Count: %{count}"

[error]
unknown_action = "No action named '%{name}'"
too_deep = "Action '%{name}' calls other actions too deeply"
hotkey = "Cannot register the hotkey: %{error}"
hook = "Cannot install the input hooks: %{error}"
run = "Cannot start %{program}: %{error}"
set_text = "Cannot replace the input's text: %{error}"
no_input = "No input to put the text in"
tray = "Cannot add the tray icon: %{error}"
input_box = "Cannot show the input box: %{error}"
library = "Error: %{detail}"

[fatal]
no_config_path = "--config needs a path"
not_running = "grapnel is not running: %{error}"
window = "Cannot create the window: %{error}"
already_running = "grapnel is already running (%{error})"
config = "Cannot load the settings:"
new_config = "# grapnel settings. See docs/spec.md for the format."

[reload]
done = "Settings loaded"
failed = "Cannot load the settings"
more = "(%{count} more)"

[ui]
title = "grapnel settings"
file = "File to edit"
save = "Save"
save_apply = "Save and apply"
add = "Add"
delete = "Delete"
drag = "Drag (or press ↑/↓) to reorder"
not_a_number = "Enter a whole number of 0 or more (empty means unset)"
kind = "Kind"
keys_or_action = "Keys / action name"
steps = "Action sequence"
add_step = "Add action"
language_auto = "Windows display language"

[ui.status]
loaded = "Loaded"
save_failed = "Not saved. Check the errors."
saved = "Saved"
saved_with_errors = "Saved (at least partly), but there are errors. Check them below."
applied = "Saved and told grapnel to reload"
apply_failed = "Cannot reach grapnel (is it running?): %{error}"

[ui.tab]
imports = "Imports"
keymap = "Default keymap"
modes = "Mode keymaps"
keyswap = "Key layout"
modifiers = "Custom modifiers"
targets = "Targets"
actions = "Actions"
others = "Other"

[ui.tab_hint]
imports = "Reads other configuration files. Absolute paths, relative paths and globs with \"*\" are allowed."
keymap = "The keymap shared by all modes."
modes = "Keymaps that only work while their mode is active."
keyswap = "Makes a pressed key count as another key. The keymap sees the swapped key."
modifiers = "Turns any key into a user-defined modifier for use in the keymap."
targets = "Defines filters by executable name, window title and so on, to limit where actions apply."
actions = "Defines what is actually input after remapping, for the keymap to call."
others = "Global settings for other behaviour. Only the entry file can set them."

[ui.general]
entry_only = "Can only be set in the entry file."
path = "File path"
browse = "Browse..."
path_kind = "Path form"
absolute = "Absolute path"
relative = "Relative path"
no_relative = "Cannot be relative: another drive (or share) than the file being edited"
delete_settings = "Delete settings from this file"
passthrough = "passthrough (targets)"
language = "language (display language)"
layout = "layout (keyboard layout of symbol key names)"

[ui.layout]
jis = "Japanese (JIS)"
us = "English (US)"
uk = "English (UK)"
de = "German"
fr = "French"

[ui.name]
mode = "Mode name"
modifier = "Modifier name"
target = "Target name"
action = "Action name"

[ui.target]
add = "Add a condition…"
kind_raw = "Raw"
kind_glob = "glob (* and ? allowed)"
kind_re = "re (regular expression)"
app = "app (exe name or full path of the foreground app)"
title = "title (title of the foreground window)"
class = "class (class name of the foreground window)"
control = "control (class name of the focused control)"
uia_id = "uia_id (AutomationId of the focused UI Automation element)"
uia_name = "uia_name (Name of the focused UI Automation element)"
uia_type = "uia_type (ControlType of the focused UI Automation element)"
not = "not (matches when this target does not)"
any = "any (matches when any of these targets does)"
all = "all (matches when all of these targets do)"

[ui.hint]
none = "none"
unlimited = "0 (no limit)"
original_input = "the original input"
inherit = "same as the parent"
replay = "none (replay the pending input)"
typed_action = "the action named by the input"

[ui.mode]
block_unmapped = "Swallow unmapped keys"
count = "Digits are a repeat count for the next binding"
unmapped_to = "unmapped_to (mode to switch to on unmapped input)"
hold = "hold (modifiers held down in this mode)"
keymap = "Keymap for this mode only"

[ui.modifier]
key = "key (the key)"
tap_none = "Send nothing when pressed alone"
tap = "tap (sent when pressed alone)"
tap_timeout_ms = "tap_timeout_ms (longest press still counted as alone, in ms)"
as = "as (modifiers held down (C, M, S, W))"

[ui.keymap]
extended = "Extended options"
node = "Wait for the next input"
press = "press (behaviour while held)"
press_default = "Default (hold)"
press_hold = "Held (hold)"
press_tap = "Once (tap)"
inherit = "Same as parent"
on_mismatch = "on_mismatch (when no binding matches)"
mismatch_default = "Default (replay)"
mismatch_replay = "Send as is (replay)"
mismatch_discard = "Discard (discard)"
mismatch_fallback = "Given input (fallback)"
timeout_ms = "timeout_ms (time to wait for the next key, in ms)"
fallback = "fallback (sent when there is no action for this app)"
fallback_none = "Send nothing as fallback"
keep_mods = "keep_mods (hold output modifiers until the input modifiers are released)"
repeat = "repeat (remember as the last change to repeat)"
targets = "targets (when this remapping applies)"
options = "Key sequence settings"
add_key = "Add key"

[ui.keyswap]
from = "From"
to = "To"
add = "Add key"

[ui.step]
text = "Type text"
set_text = "Replace the input's text (UI Automation)"
mouse_move = "Move mouse (relative)"
mouse_move_to = "Move mouse (absolute)"
sleep = "Wait"
run = "Run program"
call = "Call action"
mode = "Switch mode"
control = "Control grapnel"
input = "Show input box"
into = "into (name of the input to write to, waited for)"
into_any = "the focused input"
text_label = "text ({arg} = argument)"
then = "then ({arg} = the typed text)"
arg = "arg ({arg} = the current argument)"
position = "position (where the input box appears)"
center = "Center"
bottom = "Screen bottom (full width)"
suspend = "Toggle suspend"
repeat = "Repeat the last change"
reload = "Reload"
exit = "Exit"

[ui.action]
by_target = "Specify per target"
by_target_hint = "Evaluated top to bottom; the first matching target's output is used (* always matches)."
add_target = "Add target"
add = "Add action"

[ui.theme]
auto = "Auto"
light = "Light"
dark = "Dark"

[ui.menu]
file = "&File"
open = "&Open…"
reload = "&Reload from disk"
save = "&Save"
save_apply = "Save and &apply"
exit = "E&xit"
view = "&View"
theme = "&Theme"
language = "&Language"
help = "&Help"
docs = "&Configuration reference"
about = "&About grapnel-settings"
about_text = "grapnel-settings %{version}\nSettings editor for grapnel."

[ui.bar]
unsaved = "Unsaved changes"
errors = "%{count} error(s)"
running = "grapnel is running"
not_running = "grapnel is not running"

[keys]
unknown = "unknown key '%{key}'"
no_direction = "gesture '%{key}' has no direction"
bad_direction = "invalid gesture direction '%{dir}' in '%{key}'"

[config]
no_files = "no configuration files"
entry_only = "only allowed in the entry file"
duplicate = "already defined in %{file}"
too_many_modifiers = "too many modifiers (%{count} > %{max})"
unknown_mode = "unknown mode '%{name}'"
unknown_layout = "unknown keyboard layout '%{name}' (use jis, us, uk, de or fr)"
unknown_target = "unknown target '%{name}'"
unknown_action = "unknown action '%{name}'"
unknown_action_or_key = "unknown action or key '%{name}'"
unknown_option = "unknown option '%{name}'"
cannot_send = "'%{key}' cannot be sent"
hotkey = "must be C/M/S/W plus a keyboard key"
real_mods = "'%{value}' must be C/M/S/W joined with '-'"
empty_keys = "empty key sequence"
is_modifier = "'%{key}' is a modifier; use it as C-/M-/S-/W-"
is_user_modifier = "'%{key}' is a user modifier key; use its tap setting"
modifier_name = "name must be alphanumeric, start with a letter and not be C/M/S/W"
modifier_key = "cannot be Ctrl/Alt/Shift/Win, a wheel or a gesture"
bad_pattern = "invalid pattern: %{error}"
circular = "circular reference"
keep_mods = "the last chord of the key needs a modifier"
keyswap_from = "must be one keyboard key, alone or with S-"
keyswap_to = "must be one keyboard key, with or without modifiers"
already_bound = "already bound at %{file}: %{at}"
unreachable = "unreachable because %{file} binds its prefix at %{at}"
read = "cannot read: %{error}"
syntax = "invalid TOML: %{error}"
not_found = "file not found"
internal = "internal error: %{error}"
not_loaded = "not loaded, so it cannot be saved"
write = "cannot write: %{error}"
